# 幸运大转盘 Feature 迭代提示词

你是一名资深全栈工程师。请在充分阅读当前仓库后，完成下面的 Feature 迭代。不要只给方案或示例代码；应直接修改项目、完成必要测试，并给出可复核的交付结果。

## 本轮 Feature 需求

请将本节替换为本轮的具体需求，至少写清：

- 目标用户与使用场景：`[待填写]`
- 需要实现的业务能力：`[待填写]`
- 关键交互或接口行为：`[待填写]`
- 明确不在本轮范围内的内容：`[待填写]`
- 验收条件：`[待填写]`

若需求描述存在不影响整体方向的细节缺失，请基于现有产品行为作最小、合理且可回退的假设，并在交付说明中列出；只有会显著改变数据模型、外部接口或用户流程时才向我确认。

## 项目背景

这是一个前后端分离的在线抽奖系统，已有转盘抽奖、九宫格抽奖、奖品管理、用户管理、抽奖记录和奖品领取等能力。

- 前端目录：`frontend/`
- 后端目录：`backend/`
- 本地编排：根目录 `docker-compose.yml`
- 前端访问端口：`3000`
- 后端及 REST API 端口：`8000`
- 数据库：MySQL，默认库名 `lottery_db`

开始编码前，先阅读 `README.md`、构建文件以及与需求直接相关的 Controller、Service、Repository、DTO、Vue 页面、Pinia Store 和 API 封装。以实际代码为准，不要仅依据 README 猜测实现。

## 技术栈约束

### 前端

- 使用现有的 Vue `3.4+`、Vite `5`、JavaScript ES Modules，不迁移到 React、TypeScript、Nuxt 或其他框架。
- 使用 Vue Composition API 和 `<script setup>`，页面组件放在 `frontend/src/views`，可复用组件放在 `frontend/src/components`。
- 路由继续使用 Vue Router `4`，跨页面共享状态继续使用 Pinia `2`；不要为局部状态滥用全局 Store。
- HTTP 请求必须通过 `frontend/src/api/index.js` 中的 Axios 实例统一封装，复用其 `/api` 基础路径、统一响应处理和错误提示；组件内不得直接创建 Axios 实例或散落原始请求。
- UI 优先复用 Element Plus `2.4+` 和 `@element-plus/icons-vue`，样式继续使用现有 SCSS 与 CSS 变量，不引入第二套 UI 框架或重复的图标库。
- 新增页面必须兼顾桌面端和移动端，覆盖加载、空数据、失败、无权限、禁用及重复提交等必要状态；异步操作用 `try/catch/finally` 保证 loading 状态可恢复。
- 不随意升级依赖或新增依赖。确需新增时，先说明现有依赖无法满足的原因，并选择维护活跃、体积适当且与当前版本兼容的包。

### 后端

- 使用 Java `17`、Spring Boot `3.2.0`、Spring Web、Spring Data JPA、Jakarta Validation、Lombok 和 Maven；不得降级 Java/Spring Boot，也不要替换为其他后端框架或 ORM。
- 持久化数据库保持 MySQL `8.0` 兼容；测试可使用项目已有的 H2。SQL、字段类型、索引与查询必须同时考虑 MySQL 行为和数据量增长。
- REST 接口继续使用 `/api` 前缀，Controller 返回统一的 `ApiResponse<T>`，前端依赖 `code === 200` 判断业务成功，不得绕开这一契约。
- 请求参数必须在系统边界完成 Jakarta Validation；业务错误抛出 `BusinessException`，由 `GlobalExceptionHandler` 统一转换，禁止在各 Controller 中重复拼装异常响应或向客户端暴露堆栈。
- Controller 只负责 HTTP 参数接收、校验和响应组装；业务规则与事务放在 Service；数据查询和原子更新放在 Repository；禁止 Controller 直接访问 Repository。
- Entity 仅表达持久化模型，接口出入参优先使用 DTO；不要把敏感字段、内部字段或完整实体直接暴露给前端。
- 所有涉及“抽奖次数、奖品库存、抽奖记录、领取状态”等多数据写入操作必须有清晰事务边界。必须考虑并发请求，使用条件更新、受影响行数校验或合适的锁，避免超卖、负库存、重复扣减、重复领取和重复提交。
- 日志使用 SLF4J 参数化写法。记录关键业务标识和结果，但不得记录密码、完整手机号、令牌等敏感信息。
- 新接口或变更接口需维护 Springdoc OpenAPI 注解，分页沿用 Spring Data 的零基页码语义，时间继续使用项目现有的 `LocalDateTime` 与统一 Jackson 格式。

### 部署与兼容性

- 保持现有前后端分离、Nginx、Spring Boot、MySQL 三服务结构及 Docker Compose 启动方式可用。
- 配置值优先通过环境变量注入，不硬编码不同环境的主机、端口、账号或密码。
- 除非 Feature 明确要求，不修改既有端口、基础 API 路径、数据库表语义和已有接口响应字段；必要变更应尽量向后兼容。

## 架构约束

遵循现有调用方向，不允许反向依赖或跨层访问：

```text
Vue View / Component
        |              Pinia（仅跨页面状态）
        v
frontend/src/api（Axios 统一入口）
        |
        v
Controller -> Service -> Repository -> MySQL
                   |
                  DTO / Entity
```

- 优先在现有模块内扩展。只有新职责具有独立业务含义且能降低复杂度时才新增模块，不要为简单逻辑制造空壳抽象。
- 保持抽奖结果与前端奖品展示顺序的一致性。若 Feature 会改变奖品筛选、排序、概率或库存规则，必须同时验证转盘和九宫格两种模式。
- 数据库变更必须明确字段约束、默认值、索引、历史数据兼容方式和回滚影响。不能仅依赖修改 JPA Entity 后由 `ddl-auto: update` 猜测生产迁移结果；若本轮确有结构变更，提供可审查的迁移 SQL。
- 权限校验必须在服务端执行，前端路由守卫和按钮隐藏只能改善体验，不能充当安全边界。普通用户不得调用管理员能力，也不得通过篡改 `userId` 操作他人数据。
- 不做与本 Feature 无关的全局重构、视觉改版、依赖升级、批量格式化或文件重命名；保持改动小而完整。

## 代码规范约束

### 通用规范

- 遵循仓库已有命名、目录布局、缩进和注释语言；新增代码使用清晰的业务命名，避免 `data1`、`temp`、`handleThing` 等含糊名称。
- 一个方法只承担一个明确职责。优先早返回降低嵌套，不复制业务规则，不保留死代码、调试输出、注释掉的大段代码或无主 TODO。
- 注释解释“为什么”以及不明显的约束，不复述代码。所有文本文件保持 UTF-8。
- 对空值、边界值、非法状态、网络失败、并发冲突和重复操作进行显式处理；禁止用静默失败掩盖错误。
- 不在代码中新增明文凭据、测试账号隐私数据或环境专用秘密。

### Java 规范

- 包名保持 `com.lottery`，类名使用 PascalCase，方法/字段使用 camelCase，常量使用 UPPER_SNAKE_CASE。
- 使用构造器注入，沿用 Lombok `@RequiredArgsConstructor`；禁止字段注入。
- DTO 和实体的校验规则应与业务规则一致。Repository 查询方法或 JPQL 要表达清楚筛选及排序条件。
- `@Transactional` 放在 Service 的写操作边界；只读逻辑不无意义地开启写事务。事务方法内的异常不得被吞掉导致部分提交。
- 禁止返回 `null` 集合；不要直接使用 `System.out`，不要用通用 `Exception` 表示可预期业务失败。

### Vue / JavaScript 规范

- 组件名使用 PascalCase，变量和函数使用 camelCase；布尔变量采用 `is`、`has`、`can` 等可读前缀。
- 使用 `ref`、`reactive`、`computed` 等 Composition API 管理状态；派生值优先使用 `computed`，不要维护可由已有状态计算出的重复状态。
- Props、Emits 和模板事件命名保持一致；可复用业务逻辑达到实际复用需求时再抽取 composable。
- 不直接修改 Props，不滥用深度监听，不在模板中堆放复杂表达式；定时器、监听器及外部事件在组件卸载时清理。
- 所有用户可触发的写操作应防重复提交；破坏性操作需要明确确认，并在失败时恢复乐观更新造成的本地状态变化。
- 样式优先复用 `frontend/src/styles/main.scss` 的主题变量。组件样式使用 `scoped`，避免无约束的全局选择器和 `!important` 扩散。

## 实施流程

1. 阅读相关代码，简述现状、受影响链路、风险与最小实现方案。
2. 列出待修改文件及每个文件的职责，确认不会越过上述架构边界。
3. 先补充或编写能体现验收行为的测试，再实现 Feature；至少覆盖主流程、关键边界和失败路径。
4. 完成前后端实现、必要的数据迁移、接口文档和少量必要的项目文档更新。
5. 执行验证，修复由本次改动引入的所有错误；不要通过删除测试、降低断言、跳过校验或隐藏异常来获得通过。
6. 检查 `git diff`，确认没有无关改动、生成物、密钥或调试代码。

## 最低验证要求

根据实际改动执行适用项，并在最终答复中给出命令与结果：

```powershell
# 后端测试与构建
Set-Location backend
./mvnw test
./mvnw package -DskipTests

# 如果仓库未提供 Maven Wrapper，则使用
mvn test
mvn package -DskipTests

# 前端构建
Set-Location ../frontend
npm install
npm run build

# 跨服务或部署配置有改动时
Set-Location ..
docker compose config
docker compose up --build
```

- 后端新增或修改业务规则：必须有对应的 JUnit 5 测试，优先使用 Spring Boot Test、MockMvc 或合适的切片测试，测试数据不得依赖执行顺序。
- 前端当前未配置测试框架：不得伪造测试结果。至少完成生产构建，并对受影响页面进行实际交互验证；若引入前端测试工具，必须说明成本和必要性。
- 涉及抽奖或库存：至少验证无库存、无次数、禁用用户、概率边界及并发扣减。
- 涉及权限或用户数据：至少验证未登录、普通用户、管理员以及越权访问。
- 涉及响应式页面：验证常见桌面宽度和移动端宽度，无文字溢出、控件遮挡或横向滚动。

## 完成定义

只有同时满足以下条件才算完成：

- 本轮验收条件全部实现，主流程可实际使用，不以 mock 数据代替真实链路。
- 前后端接口字段、错误行为和状态更新一致，原有转盘与九宫格功能无回归。
- 关键业务规则由后端强制执行，事务和并发风险已处理。
- 相关测试与构建通过；无法执行的验证必须明确说明原因和剩余风险。
- 没有无关重构、明文秘密、调试代码、生成目录或未说明的破坏性变更。

## 最终答复格式

完成后仅按以下结构汇报，内容具体、简洁，不要只说“已完成”：

1. **实现摘要**：面向用户说明 Feature 现在能做什么。
2. **关键改动**：列出主要文件及其职责，说明关键业务规则、事务/权限处理。
3. **接口与数据变更**：列出新增或变化的接口、字段、迁移；无变化则明确写“无”。
4. **验证结果**：逐条列出执行的命令、是否通过及覆盖场景。
5. **假设与剩余风险**：列出实现中采用的假设、未执行验证或后续事项；没有则写“无”。
